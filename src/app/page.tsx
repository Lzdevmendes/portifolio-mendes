export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p
        style={{
          fontFamily: "var(--font-syne)",
          color: "var(--color-teal)",
          fontSize: "1.25rem",
          letterSpacing: "0.1em",
        }}
      >
        lzmen — portfolio loading...
      </p>
    </main>
  );
}
